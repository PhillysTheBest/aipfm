apiVersion: v1
kind: Secret
metadata:
  name: aipfm-secrets
type: Opaque
stringData:
  # In production, use a real secret management solution or seal these
  SECRET_KEY: "django-insecure-prod-key"
  DB_PASSWORD: "postgres"

